import { Card } from '@mui/material';
import React from 'react';

const Loginpage = () => {
    return (
        <Card>
            <h1>{"Login!"}</h1>
        </Card>
    );
};

export default Loginpage;
